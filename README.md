- создать маршруты для работы с событиями (добавить, удалить, изменить)
- создать модель события (название '', описание {}, тип '', время (date), продолжительность (сек number), фото (от 0 до 5), адрес строка, review [пустой], user_id (id тип id), )
- маршрут для получения всех событий (добавить элементарную фильтрацию (по названию, по описанию))

